---
// Section — a vertical chunk of the page.
// Two sections in a row get consistent spacing between them
// (handled by .section + .section in global.css).
// Use it as the outer wrapper for any logical "block" on a page.
---

<section class="section">
    <slot />
</section>
